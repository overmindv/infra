apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task-hunter-postgres-data
  namespace: overmindv
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-hunter-postgres
  namespace: overmindv
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: task-hunter-postgres
  template:
    metadata:
      labels:
        app: task-hunter-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: task_hunter
            - name: POSTGRES_USER
              value: task_hunter
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: overmindv-secrets, key: TASK_HUNTER_POSTGRES_PASSWORD } }
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U task_hunter -d task_hunter"]
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U task_hunter -d task_hunter"]
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: task-hunter-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: task-hunter-postgres
  namespace: overmindv
spec:
  selector:
    app: task-hunter-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres

